const arr = [10, 20, 30, 40];
arr.push(50); // 배열 맨 뒤에 추가
console.log(arr);
arr.pop(); // 배열 맨 뒤에 요소 추출
console.log(arr);
arr.unshift(0); // 배멸 맨 앞에 추가
console.log(arr);
arr.shift(); // 배열 맨 앞에 요소 추출
console.log(arr);
